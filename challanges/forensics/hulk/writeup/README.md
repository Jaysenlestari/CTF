# Writeup <hulk>

1. vol -> banners
2. vol -> linux.bash
3. vol -> linux.pagecache.Files 
4. vol  -> -f dump -o ./output linux.pagecache.InodePages --find /home/tipsen/Downloads/netsos --dump -> hash
5. rev binaryny
6. bulk extractor -> aeskey.txt & pcap
7. sama kyk 4 grep ".enc"
8. decrypt aja ada di file mydiary